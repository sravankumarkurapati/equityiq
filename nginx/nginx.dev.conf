# nginx/nginx.dev.conf — LOCAL DEVELOPMENT
#
# Routes all traffic through one port (80):
#   /api/* → FastAPI  (port 8000)
#   /*     → Streamlit (port 8501)
#
# This mirrors production so local testing is accurate.

events {
    worker_connections 1024;
}

http {

    # Upstream service names match docker-compose service names
    upstream fastapi_backend {
        server fastapi:8000;
    }

    upstream streamlit_frontend {
        server streamlit:8501;
    }

    server {
        listen 80;
        server_name localhost;

        # Route /api/* to FastAPI
        # Strip /api prefix before forwarding
        location /api/ {
            rewrite ^/api(/.*)$ $1 break;
            proxy_pass http://fastapi_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            # Long timeout for agent runs (up to 3 minutes)
            proxy_read_timeout 180s;
            proxy_connect_timeout 10s;
        }

        # Route everything else to Streamlit
        location / {
            proxy_pass http://streamlit_frontend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            # WebSocket support — required for Streamlit live updates
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            # Keep WebSocket alive for long sessions
            proxy_read_timeout 86400;
        }
    }
}